<config>
  <executableName>${app.executable}</executableName>
  <mainClass>${app.mainclass}</mainClass>
  <os>ios</os>
  <arch>x86_64</arch>
  <resources>
    <resource>
      <directory>../forge-gui</directory>
      <includes>
        <include>res/**</include>
      </includes>
    </resource>
    <resource>
      <directory>res</directory>
    </resource>
  </resources>
  <target>ios</target>
  <iosInfoPList>Info.plist.xml</iosInfoPList>
  <forceLinkClasses>
    <pattern>org.apache.xalan.processor.TransformerFactoryImpl</pattern>
    <pattern>java.util.logging.**</pattern>
    <pattern>com.esotericsoftware.minlog.Log</pattern>
    <pattern>com.badlogic.gdx.**</pattern>
  </forceLinkClasses>
  <!--
    The <libs> block is intentionally absent here.
    libgdx.a / libObjectAL.a / libgdx-freetype.a contain only armv7 and arm64 *device*
    slices; they have no simulator slices.  Linking them into a simulator binary causes
    the linker to fail with "building for iOS Simulator, but linking in object file
    built for iOS".  The iOS Simulator does not need these JNI libraries at runtime —
    OpenGL ES and audio are provided by the host Metal/GLES translation layer and
    CoreAudio, which RoboVM/MobiVM links automatically via the <frameworks> listed
    below.  The device build (robovm.xml) still includes them.
  -->
  <frameworks>
    <framework>UIKit</framework>
    <framework>OpenGLES</framework>
    <framework>QuartzCore</framework>
    <framework>CoreGraphics</framework>
    <framework>OpenAL</framework>
    <framework>AudioToolbox</framework>
    <framework>AVFoundation</framework>
    <framework>CoreMotion</framework>
  </frameworks>
</config>
