<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>${app.name}</string>
    <key>CFBundleExecutable</key>
    <string>${app.executable}</string>
    <key>CFBundleIdentifier</key>
    <string>${app.id}</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>${app.name}</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>${app.version}</string>
    <key>CFBundleSignature</key>
    <string>????</string>
    <key>CFBundleVersion</key>
    <string>${app.build}</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UIDeviceFamily</key>
    <array>
        <integer>1</integer>
        <integer>2</integer>
    </array>
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
    </array>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>CFBundleIcons</key>
    <dict>
        <key>CFBundlePrimaryIcon</key>
        <dict>
            <key>CFBundleIconFiles</key>
            <array>
                <string>Icon</string>
                <string>Icon-72</string>
            </array>
        </dict>
    </dict>
    <!--
      UIRequiresFullScreen: games must opt out of iPad split-screen / slide-over.
      Without this key the app can be windowed on iPad, which is not supported by
      a libGDX game using OpenGL ES in full-screen mode.
    -->
    <key>UIRequiresFullScreen</key>
    <true/>
    <!--
      MinimumOSVersion: target iOS 12+ to match the RoboVM/libGDX toolchain and
      ensure modern API availability.  Without this the binary carries no explicit
      minimum, which can suppress OS-version-gated optimisations.
    -->
    <key>MinimumOSVersion</key>
    <string>12.0</string>
    <!--
      CADisableMinimumFrameDurationOnPhone: allows the app to render at the full
      ProMotion refresh rate (120 Hz) on iPhone 13 Pro and later.  Without this
      key iOS caps rendering at 60 fps on high-refresh-rate displays.
    -->
    <key>CADisableMinimumFrameDurationOnPhone</key>
    <true/>
    <!--
      UILaunchStoryboardName: declares the launch screen storyboard for the app.
      Without this key, iOS runs the app in compatibility mode and adds black bars
      at the top and bottom of modern iPhone screens (anything larger than 3.5").
      The storyboard is compiled to a .storyboardc bundle by ibtool during the
      IPA build.  The value must match the .storyboard filename without extension.
    -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>
</dict>
</plist>
