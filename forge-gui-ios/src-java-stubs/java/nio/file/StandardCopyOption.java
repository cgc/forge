package java.nio.file;

/** Stub for MobiVM: java.nio.file is absent from the MobiVM runtime. */
public enum StandardCopyOption implements CopyOption {
    REPLACE_EXISTING,
    COPY_ATTRIBUTES,
    ATOMIC_MOVE;
}
