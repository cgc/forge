package io.sentry.protocol;

/**
 * Stub replacement for {@code io.sentry.protocol.SentryId}.
 *
 * <p>Returned by {@link io.sentry.Sentry#captureException(Throwable)} and
 * {@link io.sentry.Sentry#captureMessage(String)} in Sentry SDK 8.x.  Forge
 * code discards the returned value, so this stub only needs to exist as a
 * type; it carries no state.
 */
public final class SentryId {
}
